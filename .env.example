# ===========================================
# Environment Variables for Job Auto-Applier
# ===========================================
# Copy this file to .env and fill in your values

# Database Configuration (PostgreSQL)
# -----------------------------------
# Docker Compose provides this automatically via docker-compose.yml
# For local development without Docker, use:
DATABASE_URL=postgresql://jobapplier:jobapplier123@localhost:5432/auto_applier

# For Railway deployment:
# Railway auto-injects DATABASE_URL when you link a PostgreSQL service

# AI API Keys (required)
# ----------------------
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: OpenAI API Key (for future use)
# OPENAI_API_KEY=your_openai_api_key_here

# Email Notification Settings (Optional)
# --------------------------------------
# For Gmail SMTP (requires App Password with 2FA enabled):
# EMAIL_SMTP_SERVER=smtp.gmail.com
# EMAIL_PORT=465
# EMAIL_USERNAME=your_email@gmail.com
# EMAIL_PASSWORD=your_app_password
